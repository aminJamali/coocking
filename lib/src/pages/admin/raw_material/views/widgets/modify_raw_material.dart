import 'package:flutter/material.dart';

class ModifyRawMaterial extends StatelessWidget {
  const ModifyRawMaterial({final Key? key}) : super(key: key);

  @override
  Widget build(final BuildContext context) => Container();
}
