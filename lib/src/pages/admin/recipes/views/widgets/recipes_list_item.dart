import 'package:flutter/material.dart';

class RecipesListItem extends StatelessWidget {
  const RecipesListItem({final Key? key}) : super(key: key);

  @override
  Widget build(final BuildContext context) => Container();
}
